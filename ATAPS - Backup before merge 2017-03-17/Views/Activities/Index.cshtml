@model List<ATAPS.Models.DisplayObject.ActivityDisplayObject>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create", new { filter = ViewBag.FilterID })
</p>
<table class="table">
    <tr>
        <th>
            Name
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Activity.ActivityName)
        </td>
        <td>

            <a href="../Activity/Edit/@item.Activity.ID?filter=@item.Agenda.ID">Edit</a> |

            <a href="../Activity/Details/@item.Activity.ID?filter=@item.Agenda.ID">Details</a> |

            <a href="../Activity/Delete/@item.Activity.ID?filter=@item.Agenda.ID">Delete</a>

            @*@Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })*@
        </td>
    </tr>
}

</table>
